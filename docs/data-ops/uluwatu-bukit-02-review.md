# Uluwatu–Bukit batch 02 — operator review

Status: ready for operator review; not ready for import dry-run; forbidden to publish.

Nine records contain partial item-level official menu transcriptions. Son of a
Baker is blocked on an inaccessible social-only identity. Current first-party
evidence resolves `ulu-artisan-ungasan` to Artisan Ungasan, but the repo name
and review publication state must be reconciled by an operator.

Tropical Temptation's official map still carries the old Cattamaran identity
and was excluded. Sundays prebooking is VIP-specific; the regular beach pass is
walk-in. Currency remains null for sources that show only raw `K`/numeric
values without an explicit currency legend. No media was collected.
