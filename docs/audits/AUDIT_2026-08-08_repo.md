# Аудит (репо-среда): роли 5–8 по docs/AUDIT_GENERAL_TZ.md

```yaml
дата: 2026-08-08
среда: A — репозиторий + база (read-only и dry-run); живой сайт недоступен (прокси)
ветка: claude/new-session-wsx3wf, диф к main — только сам ТЗ-документ
база: bali-privilege / egkdapqwkfprtyqvvnso
исправлений в код/базу: ноль — аудит ничего не чинит (правило 7 ТЗ)
```

Прочитан свежий `docs/HANDOFF_2026-08-08.md` (правило промпта A). Ловушки 1–10
пройдены все; по каждой ниже есть строка — включая те, где всё чисто.

---

## Сводка

| Серьёзность | Находок |
| --- | --- |
| P1 | 4 |
| P2 | 4 |
| P3 / watch | 3 |
| Чисто (проверено и в порядке) | 9 |
| Не проверено | 1 |

---

## P1

**[Инженер][P1] Кнопка героя главной и футер ведут на `/together` — маршрута не существует.**
Доказательство: прочитано — `app/page.tsx:207` и `components/SiteFooter.tsx:95`
ссылаются на `/together`; `find app -path "*together*"` пуст; `proxy.ts` этот
путь не переписывает.
Дата: 2026-08-08. Противоречит: веб-аудит 08.08 видел на `/together` заглушку
«Unpacking…» — с живого сайта это выглядит иначе, чем из репозитория, но обещание
кнопки не выполняется в обоих случаях. Третья CTA героя — самое видное место
сайта.

**[Инженер][P1] 22 теста из 116 не зарегистрированы ни в одном npm-скрипте и не
запускаются нигде.**
Доказательство: посчитано — сверка `**/*.test.*` с `package.json` (глоб-паттерны
раскрыты); `ci.yml:56-57` гоняет `npm test`, других запусков тестов в CI нет.
Среди незапускаемых — security-значимые: `lib/redemption-token.test.ts`,
`lib/guest-server.test.ts`, `lib/admin-auth.test.ts`,
`lib/source-attribution.test.ts`, `lib/supabase/server.test.ts`.
Исполнено: пробный запуск `lib/redemption-token.test.ts` — 2/2 pass. Тесты
живые; они просто никогда не выполняются. Тот же класс, что найденный ранее
`event-store.test.ts` (ловушка 3), но 22 файла.
Полный список — в приложении A.

**[Инженер][P1] Очередь freshness в админке молча теряет хвост двух таблиц —
потолок PostgREST уже превышен.**
Доказательство: посчитано — `venue_action_capabilities` 1192 строки, `venues`
1440 строк (одна строка = строка таблицы); прочитано —
`app/admin/(protected)/freshness/data.ts:79` читает обе без `.range()`/лимита,
PostgREST отдаёт максимум 1000. Оператор не видит ~192 capability-записей и
~440 названий заведений (имена падают в слаг). Ловушка 1, повторившаяся в
админке через четыре дня после починки той же ошибки в каталоге.
Дата: 2026-08-08.

**[Аудитор данных][P1] «Заполнено ≠ пригодно» по всему острову: рупиевая цена
есть у 3% каталога.**
Доказательство: посчитано — среди 1214 живых карточек `price_anchor` непуст у
454, но цифры содержат 38, а 370 — только значки `$$`. Числовые
`price_min_idr`/`price_max_idr`/`price_band` по Чангу — нули (замер 08.08).
Одна строка = живая карточка (`status='active'`, `publication_status='published'`).
Противоречит: любым планам, читающим «price_anchor 36–56%» как готовность.
Бриф на сбор уже написан: `docs/DATA_OPS_CANGGU_PRICES_BRIEF.md`.

## P2

**[Безопасность][P2] Дефолтные гранты `anon` на ~20 таблицах — вплоть до
DELETE/TRUNCATE на `guest_refs`, `events`, `consent_log`, `redemption_events`,
`venue_submissions`.**
Доказательство: посчитано — `role_table_grants` для `anon`; политик записи для
`anon` нет ни одной (все 11 политик — SELECT с USING-гейтами), поэтому через
PostgREST записи блокируются RLS и это не эксплуатируется. Но новые миграции
(0031+) уже снимают гранты по образцу `attribution_sources` — у старых таблиц
остался дефолт. Долг defense-in-depth: revoke по тому же образцу.
Дата: 2026-08-08.

**[Инженер][P2] `/jimbaran/best-hotels` — ссылка из хаба на несуществующий
маршрут.**
Доказательство: прочитано — `app/jimbaran/page.tsx:43` ссылается;
`app/jimbaran/best-hotels/page.tsx` нет; `app/sanur/best-hotels/page.tsx`
существует — похоже, шаблон хаба скопирован не до конца.

**[Guardrails][P2-риск] В базе лежит сырьё чужих рейтингов: `google_rating` у
283 живых карточек, плюс `google_reviews` и `rating_source`.**
Доказательство: посчитано — 290/288/290 строк, 283 live. Граница публикации
держится: рендера нет ни одного (греп по `app/ lib/ components/` пуст),
`aggregateRating` в коде отсутствует, запрет закреплён тестом
(`scripts/publication-boundary.test.mjs:42`). Находка не «нарушение», а
инвентарь риска: данные, которые guardrail §4.2 запрещает публиковать, лежат в
одном `SELECT` от публикации, и запрос «добавить звёзды» уже случался
(скилл `otherbali-schema-markup`). Решение о судьбе колонок — за владельцем.

**[Аудитор данных][P2] Стандарт полей нарушается массово на уже заполненных
карточках.**
Доказательство: посчитано, по живым карточкам: `best_for` длиннее 90 знаков —
**309**; запрещённый заход «Travellers/Visitors who want…» — **84**; `not_for`
длиннее 80 — **75**. Стандарт: `otherbali-venue-record-standard` (≤90, ≤80,
без шаблонного захода).
Дата: 2026-08-08.

## P3 / watch

**[Аудитор данных][P3] Кластеры одинаковых часов.**
Посчитано: `Mo-Su 07:00-23:00` ×25, `07:00-22:00` ×16 и ещё восемь кластеров
по 8–11. Круглые значения правдоподобны; настороженность — `08:00-23:59` ×8
(23:59 — сигнатура полуночной усечки). По ловушке 8: при следующем прогоне
часов проверять построчно, групповая правка запрещена.

**[Аудитор данных][P3] Число `editorial_status='published'` сдвинулось за
сутки: 160 → 187.**
Посчитано 2026-08-08. Противоречит: `HANDOFF_2026-08-08.md` называет 160 (по
передатчику Other Bali). Не ошибка — работа продолжается; но это наглядная
цена недатированных чисел (правило 5 ТЗ).

**[Безопасность][P3] Утверждение handoff «уведомления: код готов, ключей нет» —
вывод, а не проверка.**
Тип доказательства в handoff фактически «со слов»: наличие `RESEND_API_KEY` на
Vercel отсюда не видно (MCP-инструмент get_project падает на валидации
параметров). Косвенное свидетельство — 6 заявок непросмотрены с 16.07 — сильное,
но статус в handoff следует читать как «не проверено, косвенно ключей нет».
Противоречит: `HANDOFF_2026-08-08.md`, таблица «построено ≠ включено».

## Чисто — проверено и в порядке

1. **Гейты §18** (исполнено): lint — 0 ошибок (1 известный warning в
   `PhotoReviewPanel.tsx`), typecheck чисто, build успешен, тесты **407 / 406
   pass / 1 skip**, seo-os validate ok.
2. **RLS включён на каждой таблице `public`** (посчитано: запрос по
   `pg_class.relrowsecurity` вернул ноль таблиц без RLS).
3. **Политики `anon` — только SELECT с гейтами** published/fresh (посчитано:
   11 политик, ни одной записи).
4. **Сервисный ключ не утекает в клиент** (прочитано: `SUPABASE_SERVICE_ROLE_KEY`
   только в серверных модулях без `"use client"`; упоминание в
   `freshness/page.tsx` — текст сообщения, не использование).
5. **PII в событиях не проходит** (прочитано + исполнено ранее в сессии:
   `log_event` валидирует тип/ref/slug; payload-цепочка отбрасывает лишние поля,
   тест «url must-not-leak» — в зарегистрированных и зелёных).
6. **T11-фикс реально подключён** (прочитано: `readAllPages<Row>` в
   `lib/data.ts:716`, каталог пагинирован). Примечание аудитора: первый греп
   `readAllPages(` дал ложную тревогу — генерик `<Row>` не совпал с паттерном.
   Инструментальный урок в копилку ловушек.
7. **Остальные публичные чтения ниже потолка 1000** (посчитано: perks 124,
   plan_entries 38, menus 129, маршруты — единицы). Единственная зона выше
   потолка — админ, см. P1.
8. **Кириллицы в публичных полях нет** (посчитано: 0 из 1214 по четырём полям) —
   guardrail 15 соблюдён. **`is_sponsored` среди живых: 0** — пей-ту-ранк не
   активен (guardrail 7). **Дублей имя+район среди опубликованных: 0**
   (Room4Dessert-пара не всплывает, второй экземпляр в `review` — см. handoff).
9. **Прод-констрейнт вне миграций** подтверждён ранее в сессии dry-run'ом с
   откатом (`venues_published_requires_verification_check`) — ловушка 10,
   исполнено 08.08.

## Не проверено

- **Состояние env-гейтов на Vercel** (`NEXT_PUBLIC_ENABLE_ANALYTICS`,
  `RESEND_API_KEY`, `NOTIFY_EMAIL_TO`): инструмент get_project не принимает
  параметры (schema mismatch), из репозитория и базы значения не видны.
  Закрывается пунктами 1–3 чек-листа владельца (Промпт C).

## Скоуп /security-review

Скилл упал на собственной команде (`git diff origin/HEAD…` — в этом клоне
`origin/HEAD` не задан). Намерение исполнено вручную: диф ветки к `main` —
один markdown-файл (`docs/AUDIT_GENERAL_TZ.md`, +197), кода нет, находок нет.
Содержательная security-работа роли 6 сделана прямыми проверками базы и кода
(см. выше).

---

## Приложение A — незарегистрированные тесты (22)

`lib/actions/__tests__/resolve-actions.test.ts` · `lib/admin-auth.test.ts` ·
`lib/guest-server.test.ts` · `lib/integrations/tablepilot-environment.test.ts` ·
`lib/photo-submission-policy.test.ts` · `lib/redemption-token.test.ts` ·
`lib/source-attribution.test.ts` · `lib/supabase/server.test.ts` ·
`lib/tablepilot.test.ts` · `lib/uluwatu/venues.test.ts` ·
`scripts/admin-boundary.test.mjs` · `scripts/canggu-guide-answer.test.mjs` ·
`scripts/canggu-visual-first.test.mjs` · `scripts/data-ops-compiler.test.mjs` ·
`scripts/data-ops-import-core.test.mjs` · `scripts/menu-action-foundation.test.ts` ·
`scripts/menu-action-schema.test.mjs` · `scripts/sanur-p0-boundary.test.mjs` ·
`scripts/ubud-p0-boundary.test.mjs` · `scripts/uluwatu-p0-boundary.test.mjs` ·
`scripts/validate-admin-freshness.test.ts` · `scripts/validation-core.test.mjs`

## Следующие шаги (вне аудита, по решению владельца)

1. Завести `/together` или снять кнопку героя и строку футера — P1, одна из
   двух правок маленькая.
2. Зарегистрировать 22 теста (проверив, что все проходят) — P1.
3. Пагинировать `getFreshnessQueue` через `readAllPages` — P1.
4. Сбор рупиевых цен по брифу — P1 контента.
5. Revoke дефолтных грантов `anon` по образцу 0031 — P2, отдельной миграцией.
6. `/jimbaran/best-hotels` — создать или снять ссылку — P2.
7. Пакетная правка `best_for`/`not_for` под лимиты стандарта — P2, через
   редакционный прогон, не автозамену.
