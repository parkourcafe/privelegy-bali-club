# Прогон координат, 4 августа 2026 — запись

```yaml
задача: заполнить latitude/longitude по заведениям Чангу, Убуда и Улувату
собрано: 11 кандидатов из внутренних источников
принято: 9 (партия 1 — 4, партия 2 — 5)
отклонено: 2
UPDATE: партия 1 — ожидалось 4, получено 4; партия 2 — ожидалось 5, получено 5
осталось: 327
статус: сбор по сайтам ЗАБЛОКИРОВАН политикой egress, не завершён
```

> **Решение основателя от 2026-08-04 (внесено после первой партии).** Селена
> подтвердила: брать координаты из сохранённых ссылок Maps разрешено,
> отдельного правила, это запрещающего, нет. Оговорка по двум строкам партии 1
> снята. Решение также открыло путь к Plus Code — это партия 2, ещё пять строк.
> Противоречие между спекой и правилами приёмки снято в обоих файлах.

Отчёт по форме «что такое хорошо» из `otherbali-data-ops-run`: сколько собрано,
сколько отклонено и почему, каким было ожидаемое число строк в UPDATE и каким
оказалось фактическое, какой URL проверен на живом сайте.

---

## 1. Блокер: из этой сессии нет выхода в сеть

Сбор по сайтам заведений — основной путь спеки — выполнить нельзя.

```txt
curl https://example.com/                    → 403 CONNECT tunnel failed
curl https://nominatim.openstreetmap.org/    → 403 CONNECT tunnel failed
curl https://senimancoffee.com/              → 403 CONNECT tunnel failed
WebFetch любой из этих адресов               → 403 Forbidden
```

Это не сбой отдельного хоста: закрыто всё, включая `example.com`. Политика
egress окружения — deny-by-default. `/root/.ccr/README.md` про 403 говорит
прямо: «The destination host is not allowed by your organization's egress
policy for this session. Do not retry or route around it — report the blocked
host». Обходить не стал.

Что осталось доступным: Supabase MCP (база), npm registry, WebSearch.

**WebSearch сознательно не использован как источник координат.** Он возвращает
сводку по сниппетам, а не страницу заведения: проверить, что координата пришла
с сайта заведения, а не с TripAdvisor, невозможно, и `source_url` в строку
записать нечего. Правило приёмки №3 требует ссылку на страницу самого
заведения. Координата — то поле, где цена ошибки выше всего: неверные часы
видно (человек приехал, закрыто, он написал), неверную координату не видно
никогда — человек просто оказывается не там.

### Что нужно, чтобы разблокировать

Один из двух вариантов:

- разрешить в политике egress окружения домены заведений и
  `nominatim.openstreetmap.org`;
- либо отдать `data/data-ops/coords-worksheet-332.csv` агенту или человеку с
  доступом в сеть — файл готов, колонки под ответ пустые.

---

## 2. Что всё-таки собрано — из внутренних источников

Шаг 1 плейбука («измерить до того, как собирать») окупился и в этот раз.

| Источник | Найдено | Принято |
|---|---|---|
| Координаты в `venues.gmaps_url` (пин `!3d/!4d`) | 2 | 2 |
| `data/data-ops/compiled/venue-maps.json` (ссылка с сайта заведения) | 2 | 2 |
| **Plus Code в полях самой записи** | 5 | 5 |
| `data/data-ops/chope-607/` (платформа бронирования Chope) | 2 | **0** |

Записано — партия 1:

| slug | lat | lng | источник |
|---|---|---|---|
| copenhagen-cafe-berawa | −8.6589493 | 115.1381321 | пин в сохранённом gmaps_url |
| revolver-canggu | −8.6531518 | 115.1355510 | пин в сохранённом gmaps_url |
| santanera | −8.6518446 | 115.1310906 | santanerabali.com/book |
| ulekan-berawa | −8.6602040 | 115.1429730 | ulekanbali.com |

Партия 2 — Plus Code:

| slug | lat | lng | деревня в том же адресе |
|---|---|---|---|
| oma-jamu | −8.6406125 | 115.1433906 | Canggu / Berawa |
| jungle-flower-restaurant | −8.5706125 | 115.2649219 | Lodtunduh, Ubud |
| gains | −8.8102375 | 115.1184219 | Jl. Buana Sari, Pecatu |
| humans-cafe | −8.8190125 | 115.1601406 | Jl. Bali Cliff, Ungasan |
| la-terrazza-uluwatu | −8.8152625 | 115.0887031 | Pantai Suluban, Pecatu |

Все девять внутри рамки своего района, точность 6–7 знаков. Перед каждой
партией прогнана одна строка с откатом (`UPDATE 1`, откат чистый). Боевые
прогоны: **ожидалось 4 — получено 4**, **ожидалось 5 — получено 5**.

SQL с приложенными отклонениями: `coords-batch1-apply.sql`,
`coords-batch2-apply.sql`.

### Про Plus Code — почему это оказалось бесплатно

Пять заведений держали в собственном поле `address` (а `oma-jamu` — в
`q=`-параметре `gmaps_url`, привязанном к названию) Plus Code вида `54Q9+W93`.
Это Open Location Code, открытый стандарт: декодируется арифметикой, без сети
и без ключа API. Клетка — 3×3 метра.

Декодировал эталонной библиотекой `open-location-code`, а не своей
реализацией, и перед этим проверил саму библиотеку: взял координату
`santanera` (из партии 1, с сайта заведения), закодировал, укоротил и
восстановил обратно — расхождение 2 метра.

Каждая точка проверена трижды: рамка района, размер клетки, и совпадение
декодированной точки с деревней, названной в том же адресе. Третья проверка
важна именно потому, что рамки недостаточно — см. ниже.

### Почему две строки отклонены

`chupacabras` и `habitat-bistro`. Обе координаты **попадают в рамку Убуда** —
то есть проверка границ их не отсеяла бы. Отсеял тип источника:

- источник — `chope.co`, сторонняя платформа бронирования, тот же класс, что
  Cloudbeds и ClassPass в списке запрещённых;
- сами записи помечены `verification_status: verification_pending`,
  `recommended_action: hold`, `publication_guard.can_publish: false`;
- у `habitat-bistro` адрес в записи — «Kaamala Resort Ubud», то есть координата
  может указывать на отель, внутри которого ресторан, а не на ресторан. Это
  ровно та ошибка, что 4 августа дала `14:00` (время заселения) вместо часов
  открытия кафе.

Обе перевыясняются с сайта заведения при первом прогоне с сетью. **Решение от
4 августа про ссылки Maps их не воскрешает** — там дело было не в Google, а в
платформе бронирования и в собственных флагах записи.

У `habitat-bistro` при этом в поле `address` лежит нормальный уличный адрес
(`Jl. Bisma No.888A, Ubud`), так что строка забирается геокодированием, как
только появится сеть, — без обращения к Chope.

### Оговорка снята решением от 4 августа

Две строки (`copenhagen-cafe-berawa`, `revolver-canggu`) взяты из пина в
сохранённом `gmaps_url`, и по ним расходились два утверждённых документа:
`acceptance-rules.md` называл источник законным, `coords-collection-spec.md`
говорил «не Google Maps как таковой».

**Селена подтвердила: брать разрешено, отдельного запрещающего правила нет.**
Обе строки остаются, ограничение 30 дней к ним не применяется. Противоречие
снято в обоих файлах, чтобы не всплыло снова:
`coords-collection-spec.md` §«Правила приёмки» п.3 и
`.agents/skills/otherbali-data-ops-run/references/acceptance-rules.md`
§Coordinates. Пересмотр требует нового датированного решения.

Это же решение открыло партию 2: Plus Code лежат в тех же полях и раньше
попадали бы под ту же оговорку.

---

## 3. Измерение: спека была неточна в трёх местах

| Что | В спеке | На самом деле |
|---|---|---|
| Объём | 355 заведений | **336** (163 Чангу · 86 Убуд · 87 Улувату) |
| Годный для геокодирования адрес | «у трети», по `full_address` | **82**, и почти все — в `venues.address`, не в `full_address` |
| Сайт / Instagram | 287 / 309 | 268 / 294 |
| `google_place_id` | нет ни у одного | подтверждено: **0** |

Главное здесь — второе. Спека мерила `full_address`, где уличных адресов
всего 29. Легаси-колонку `venues.address` никто не мерил, а в ней **82**
адреса с уличным маркером — почти втрое больше. Для геокодирования брать нужно
`address`, а `full_address` в большинстве строк держит пометку района.

Ещё 46 строк несут короткую ссылку `goo.gl` / `maps.app.goo.gl`: координаты в
ней есть, но добываются только HTTP-редиректом, то есть тоже упираются в
блокер. 185 строк несут `maps/search/?api=1&query=<название>` — там координат
нет вообще, это поисковая ссылка по названию.

---

## 4. Рабочий файл на оставшиеся 327

`data/data-ops/coords-worksheet-327.csv` — одна строка на заведение, колонки
`latitude,longitude,source_url,source_type,status,note` пустые под ответ.
Отсортирован по колонке `priority`, то есть по тому, сколько работы строка
потребует:

| priority | Что есть | Строк |
|---|---|---|
| 1 | сайт заведения — открыть contact/find-us | 262 |
| 2 | короткая ссылка Maps — разрешить редирект | 3 |
| 3 | уличный адрес — геокодировать через OSM | 19 |
| 4 | только Instagram — геометка в шапке | 35 |
| 5 | ничего: ни сайта, ни IG, ни адреса | 8 |

Восемь строк priority 5 — честный `not_found` без выхода в поле; их не нужно
пытаться «дозаполнить» правдоподобным значением.

---

## 5. Код: ворота на координаты вынесены и покрыты тестами

Шаг 7 плейбука — «опубликовать поле и написать тест на отказ».

Проверка координат жила инлайном в `app/places/[slug]/page.tsx` и потому не
тестировалась. Вынесена в `publishableGeoCoordinates` в
`lib/venue-presentation.ts`, рядом с `publishableStreetAddress`, и покрыта
тестами именно на отказ:

- половина пары (`lat` есть, `lng` пустой) — не публикуется ничего; в этой
  форме сейчас находятся 332 из 336 заведений;
- не-числа, `NaN`, `Infinity`;
- точка вне диапазона Земли;
- ровно `0,0` — туда попадает пустое значение, если что-нибудь разберёт его
  как число, и это Атлантика, а не Бали.

Проверка на рамку района сюда сознательно не внесена: это правило приёмки на
входе, а не правило отдачи. Страница не должна молча прятать координату
заведения, стоящего на границе района.

`npm run lint` — 0 ошибок (1 предупреждение, унаследованное, в нетронутом
файле). `npm run build` — успешно. Тесты — 31 из 31.

---

## 6. Что не проверено

**Живая страница не проверена.** Шаг 8 плейбука требует прогнать один URL
через Rich Results Test; из сессии нет сети, значит и до `otherbali.com` не
дотянуться. Проверено только то, что цепочка от базы до разметки существует и
отдаёт `geo` при непустой паре — на уровне кода и тестов.

Проверить вручную, это одна минута:

```txt
search.google.com/test/rich-results → https://www.otherbali.com/places/santanera
```

Ожидается блок `geo` с `latitude: -8.6518446`, `longitude: 115.1310906`.
