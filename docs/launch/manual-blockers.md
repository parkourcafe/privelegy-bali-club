# Manual launch blockers

1. Identify the Vercel project serving both production domains and the duplicate project; record project IDs/names and consolidate without an unreviewed production deploy.
2. Merge the expanded workflows, configure `PREVIEW_ALLOWED_HOSTS` with the exact project-specific Vercel hostname/pattern (never broad `*.vercel.app`), run all five workflow files on GitHub, retain run URLs and configure production branch protection: required checks, one review, outdated-branch protection and no direct push. The Browser E2E workflow has two jobs: credential-free Chromium on PR/push and a separate configured data-backed proof. Repository configuration alone is not a green run.
3. Create the protected GitHub environment `otherbali-browser-staging`; restrict it to protected refs/reviewers and configure `OTHER_BALI_STAGING_SUPABASE_URL`, `OTHER_BALI_STAGING_SUPABASE_PROJECT_REF`, `OTHER_BALI_STAGING_SUPABASE_ANON_KEY`, `OTHER_BALI_STAGING_SUPABASE_SERVICE_ROLE_KEY`, and `OTHER_BALI_STAGING_GUEST_REF_SIGNING_SECRET` against one disposable isolated project. Never expose these to arbitrary PR code or the dependency-install step.
4. Create a production-like preview with that isolated Supabase project, pass `/api/health/ready` including the exact service-only v2/`0041` schema/data probe, preview smoke, real 404 checks, Mobile API bootstrap and the configured browser job without using the production database. Retain the browser report, then complete Safari, manual keyboard/screen-reader, contrast and supported-device QA; Chromium/axe is not full WCAG proof.
5. Provision a 32+ character current `GUEST_REF_SIGNING_SECRET` in the real Production/Preview deployment before release and verify readiness. Do not create `GUEST_REF_PREVIOUS_SIGNING_SECRET` unless rotating from a deployment that actually used the previous key; during a real rotation retain it for the approved cookie/data migration window.
6. Run the read-only data audit against the real production catalogue, review every manual backfill finding and confirm the highest applied production migration before applying `0035`–`0041` through the normal migration process. Preflight duplicate historical listing confirmations and back up/test restore first; do not bypass a fail-closed migration.
7. Confirm privacy controller identity, legal/consent basis, retention periods, processor terms and deletion policy, including retention for hash-only GuestRef tombstones, irreversibly anonymous event aggregates, venue-photo contact/IP/object/consent/review evidence and photo lifecycle tombstones. Approve a guide-lead retention maximum and staff same-contact verification/export/correction/deletion through `support@otherbali.com` before promoting lead capture. Obtain legal review and complete App Store privacy answers from the approved inventory.
8. Add edge/WAF controls with a measured global quota for fresh GuestRef bootstraps and bot controls for guide leads/contact surfaces. Per-cookie/GuestRef database limits do not bound repeated clean cookie jars; do not promote lead capture until the external control and response owner are evidenced.
9. Install/select full current Xcode, run the unsigned Simulator Release workflow, inspect the built binary/dependency privacy manifests and retain the `.xcresult`/privacy evidence.
10. Close the five native readiness blockers currently reported by `npm run ios:native-readiness`: `signing_team_missing`, `associated_domains_not_linked`, `associated_domains_missing`, `aasa_missing`, and `native_map_missing`. App/AppLauncher/Browser/Network/Preferences/Share integration plus local warm/cold URL handlers are implemented; they are not production Universal Link evidence.
11. Decide and configure the real Apple Developer Team ID; validate `apple-app-site-association` from production with the exact `TEAMID.com.otherbali.app` identifier.
12. Only after signing, entitlements, production AASA and the native map are complete, run signed-device cold/warm link checks and complete TestFlight clean-install/upgrade, online/offline, consent/export/delete, maps/external return, background/foreground and supported-device checks. The initial project is iPhone-only until iPad QA exists.
13. Establish measured web/mobile performance budgets from a real preview baseline; do not invent LCP/JS/image thresholds without data.
14. Replace shared admin credentials with individual auth, roles, MFA, audit log and revocation before enabling an operator surface in public production.
15. Do not apply the untracked handoff `docs/loop/handoffs/emergency_partner_token_lockdown_20260714 2.sql` in its current form: it calls unqualified `gen_random_bytes` while its SECURITY DEFINER functions pin `search_path` without the Supabase `extensions` schema. Correct, review and track it separately before using it for a production token rotation; this integration intentionally did not modify the user-owned file.
16. Before deploying `__Host-` identity cookies, audit how many production rows remain linked only to legacy unprefixed `bp_guest` bearer cookies and approve a secure user-notice/export/erasure migration. Automatic trust migration is intentionally blocked because the server cannot distinguish a legitimate legacy host cookie from sibling-domain cookie tossing; silently orphaning existing saves/redemptions/consent is not an acceptable rollout plan.
17. Authenticate operations to the exact Other Bali production Supabase project, apply `0041`, then explicitly reconcile every historical `reconcile_required` photo row against private Storage and its exact consent record. A present object requires a SHA-256 from downloaded bytes; confirmed absence must be recorded as missing. An unconsented missing row can be safely closed, while a consented missing row requires restoration/manual evidence resolution and must remain blocking. Never synthesize rights evidence or auto-publish a reachable legacy URL. Keep readiness closed until all unresolved/missing/cleanup/stale/inconsistent rows are resolved.
18. Configure `npm run photo:cleanup` in protected production operations using the reviewed runbook. Run dry-first, retain aggregate output, use an approved grace period, then run `--apply` until no eligible unconsented work remains. This crash reconciler is not a retention schedule and must not erase consented evidence.
19. Complete an owner-rights audit for the 91 live `/venue-photos/draft/` images and the 608 private official-source candidates. The latter remain `awaiting_owner_consent`; neither public reachability nor official-source collection proves exact-image publication rights.
