# Uluwatu P0 content briefs

No public page passes every publication gate in this cycle.

## `/uluwatu` — `HOLD`

- Action: `P0_UPDATE`.
- Primary intent: help a traveller understand the editorial Uluwatu area and navigate distinct decision guides.
- Blocker: the retained live page contains many factual and editorial assertions outside `CLM-ULU-PILLAR-001` through `006`. A changed-copy-only review would leave unledgered legacy claims on a touched page and violate the district publication rule.
- Next action: inventory every retained claim; refresh first-party operational sources; add allowed/prohibited wording and refresh rules; remove any claim that cannot pass. Only then create a `READY_FOR_CODEX_DRAFT` brief.
- Canonical, schema, sitemap and current route remain preserved; no new page or public copy is authorized.

## Other held P0 topics

`/uluwatu/best-restaurants`, `/uluwatu/best-brunch`, `/uluwatu/beach-clubs-sunset`, and `/uluwatu/48-hours` have approved intent owners but no brief because operational/entity/field claims are incomplete. `/uluwatu/resort-pool-day-passes` is `HOLD` and requires owner, current-term and Maps closure.

## Non-page maintenance

The homepage Uluwatu area card may be corrected to `/uluwatu` under `CLM-ULU-HOME-001`; this does not draft or publish a district page.
