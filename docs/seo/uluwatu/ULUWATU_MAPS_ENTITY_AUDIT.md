# Uluwatu Maps entity audit

Every `maps_url` and `place_id` remains null. Status for all entities: `NEEDS_MANUAL_MAPS_CONFIRMATION`.

Manual queue: confirm canonical pin, duplicate status, current name, locality and official-site match. No Maps action layer is authorized from this pack.
