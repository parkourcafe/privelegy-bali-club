-- Координаты, свободная партия — 24 записи, прошедшие проверку.
--
-- Источник: колонка venues.gmaps_url, уже лежавшая в нашей базе. Ни одна
-- строка не собиралась заново — это восстановление данных, которые импорт
-- записал в URL, но не записал в latitude/longitude.
--
-- Это шаг «бесплатные источники до заказа сбора» из
-- .agents/skills/otherbali-data-ops-run/references/acceptance-rules.md.
-- Прошлый прогон забрал так 22 строки; эти 24 оставались незамеченными,
-- потому что разделитель координат закодирован как %2C, а не запятой.
--
-- ПРИМЕНЕНО 2026-08-04. Ожидалось 24, обновлено 24.
-- Покрытие координат: 639 → 663 из 1221 опубликованных.
-- Разрыв по трём целевым районам: 343 → 336.

-- ── DRY RUN — одна строка, откат ──────────────────────────────────────
-- Выполнено до основного запроса. Доказывает, что форму записи
-- принимают ограничения, триггеры и политики.
begin;
update venues
set latitude = -8.6408826, longitude = 115.1339577
where slug = 'crate-cafe'
  and status = 'active'
  and publication_status = 'published';
-- получено: UPDATE 1, откат подтверждён (latitude снова null)
rollback;


-- ── APPLY ─────────────────────────────────────────────────────────────
-- ожидается: UPDATE 24
update venues as v
set latitude = d.lat, longitude = d.lng
from (values
  -- целевые районы (7)
  ('crate-cafe',              -8.6408826, 115.1339577),
  ('air-terjun-tegenungan',   -8.5752294, 115.2907014),
  ('anomali-coffee-ubud',     -8.5082382, 115.2652257),
  ('seeds-of-life',           -8.5083604, 115.2642967),
  ('tirta-empul',             -8.4143876, 115.3159951),
  ('garuda-wisnu-kencana',    -8.8141627, 115.1666520),
  ('pantai-pandawa',          -8.8458046, 115.1850876),
  -- остальные районы (17)
  ('desa-wisata-penglipuran', -8.4223453, 115.3593125),
  ('desa-wisata-tembuku',     -8.4335942, 115.3859581),
  ('desa-wisata-undisan',     -8.4450777, 115.3961784),
  ('pura-kehen',              -8.4418491, 115.3600201),
  ('bunut-bolong',            -8.3634089, 114.9187067),
  ('desa-wisata-medewi',      -8.3787330, 114.8231445),
  ('pantai-jimbaran',         -8.7923274, 115.1231962),
  ('desa-wisata-besakih',     -8.3575455, 115.4644903),
  ('desa-wisata-bugbug',      -8.4973091, 115.5926613),
  ('desa-wisata-tenganan',    -8.4764705, 115.5722248),
  ('pantai-kuta',             -8.7182136, 115.1687568),
  ('air-terjun-gitgit',       -8.2028961, 115.1388673),
  ('desa-wisata-gitgit',      -8.1970065, 115.1390822),
  ('pantai-lovina',           -8.1629180, 115.0221696),
  ('desa-wisata-sanur-kauh',  -8.6956574, 115.2496763),
  ('jatiluwih',               -8.3574015, 115.1187934),
  ('tanah-lot',               -8.6212286, 115.0868849)
) as d(slug, lat, lng)
where v.slug = d.slug
  -- Партия не меняет состояние публикации.
  and v.status = 'active'
  and v.publication_status = 'published'
  -- Не перезаписывать уже проверенную координату.
  and v.latitude is null;


-- ── VERIFY ────────────────────────────────────────────────────────────
-- ожидается: 24. Меньше — значит slug не совпал, а не «часть уже была верна».
select count(*) from venues
where slug in ('crate-cafe','air-terjun-tegenungan','anomali-coffee-ubud','seeds-of-life',
               'tirta-empul','garuda-wisnu-kencana','pantai-pandawa','desa-wisata-penglipuran',
               'desa-wisata-tembuku','desa-wisata-undisan','pura-kehen','bunut-bolong',
               'desa-wisata-medewi','pantai-jimbaran','desa-wisata-besakih','desa-wisata-bugbug',
               'desa-wisata-tenganan','pantai-kuta','air-terjun-gitgit','desa-wisata-gitgit',
               'pantai-lovina','desa-wisata-sanur-kauh','jatiluwih','tanah-lot')
  and latitude is not null and longitude is not null;
-- получено: 24


-- ── ПРОВЕРКА ПРИЁМКИ, применённая к каждой строке ─────────────────────
--
-- 1. Рамка района. Семь строк целевых районов проверены по границам из
--    acceptance-rules.md (canggu / ubud / uluwatu-bukit). Остальные
--    семнадцать — по общей рамке Бали (−8.95…−8.05, 114.40…115.75),
--    потому что для их районов границы не определены. Все прошли.
-- 2. Точность. У всех 24 не меньше пяти знаков после запятой.
-- 3. Источник. Координата взята из gmaps_url той же записи; ни одна не
--    получена геокодированием и ни одна не взята из агрегатора.
--
-- Отклонённых строк в этой партии нет: проверка шла до записи, и в
-- UPDATE попало только то, что её прошло. Из 495 опубликованных записей
-- без координат, у которых есть gmaps_url, координаты содержали только
-- эти 24 — у остальных 471 в ссылке лежит поисковый запрос по названию,
-- а не точка.


-- ── ВЗЯТО С ОГОВОРКОЙ ─────────────────────────────────────────────────
--
-- Все 24 — источник Google Maps.
--   Спека сбора (coords-collection-spec.md, правило 3) требует, чтобы
--   источником была страница самого заведения, и прямо исключает Google
--   Maps. Партия ей формально противоречит. Взято потому, что
--   acceptance-rules.md отдельно разрешает этот путь как бесплатный
--   источник, и прошлый прогон уже забрал так 22 строки. Существенно и
--   то, что координаты physически уже хранились в нашей базе — перенос
--   из gmaps_url в latitude/longitude не создаёт нового хранения
--   данных Google, а только переносит их в пригодную для чтения колонку.
--   Если это решение будет пересмотрено, откатывать нужно все 46 строк
--   (22 предыдущих + эти 24), а не только эту партию.
--
-- 17 строк — это площадные объекты, а не точки.
--   desa-wisata-* (туристические деревни), pantai-* (пляжи), jatiluwih
--   (рисовые террасы), air-terjun-* (водопады). Координата у них —
--   центр области, а не здание. Для объекта, который сам является
--   областью, это корректно, но правило «результат геокодера типа
--   „здание“ или „точка интереса“» к ним неприменимо, и точность пина
--   внутри области не проверялась.
--
-- last_verified_at намеренно НЕ выставлен.
--   Партия по часам (hours-batch1-apply.sql) его выставляла, и это было
--   верно: там для каждой строки открывали официальный сайт заведения в
--   тот же день. Здесь проверки записи не было — координата
--   восстановлена из данных, уже лежавших в базе. Поле уходит на
--   публичную карточку (components/VenueCard.tsx:44), на страницу места
--   (app/places/[slug]/page.tsx:468) и в lastModified карты сайта
--   (app/sitemap.ts:147), поэтому дата «проверено 2026-08-04» на 24
--   карточках, чьи описания и часы никто сегодня не смотрел, была бы
--   ложным обещанием свежести.
