-- Часы: снятие чужого значения — 3 записи из 5. ПРИМЕНЕНО 2026-08-04.
--
-- ── ЧТО БЫЛО НАЙДЕНО ──────────────────────────────────────────────────
--
-- Пять опубликованных заведений несли одну и ту же строку
-- `Mo-Su 14:00-23:59`, все со штампом last_verified_at = 2026-07-28.
--
-- ── ПЕРВЫЙ ВЫВОД БЫЛ НЕВЕРЕН ──────────────────────────────────────────
--
-- Из совпадения строки и даты был сделан вывод: «пять несвязанных
-- заведений с идентичным значением — это один плохой импорт». По этому
-- выводу все пять были обнулены. Это ошибка: два значения из пяти были
-- верными, и их пришлось восстанавливать.
--
-- Проверить было чем. В той же строке лежит `opening_hours_json` —
-- исходное посуточное значение, из которого выведена колонка
-- `opening_hours`. Достаточно было открыть его до записи:
--
--   casa-blanca-bali        json «2.00pm-12.00am»  → 14:00 ВЕРНО
--   satoshi                 json «2.00pm-12.00am»  → 14:00 ВЕРНО
--   green-spot-cafe         json «7.00am-11.00pm»  → 14:00 чужое
--   the-nini-restaurant-12  json «7.00am-11.00pm»  → 14:00 чужое
--   ecosfera-yoga-canggu    json null              → подтвердить нечем
--
-- Одинаковая строка возникла по двум разным причинам. У casa-blanca и
-- satoshi заведение действительно открывается в 14:00, а `23:59` — это
-- штатное усечение полуночи (`12.00am`), описанное в acceptance-rules.
-- У остальных 14:00 противоречит их же исходному значению.
--
-- Правило, которое из этого следует: **совпадение значения и даты — это
-- повод проверить, а не вывод.** Приёмка идёт по строке и по её
-- собственному источнику, даже когда строки выглядят как одна группа.
--
-- ── ЧТО ПРИМЕНЕНО ─────────────────────────────────────────────────────
--
-- Обнулены три строки: green-spot-cafe, the-nini-restaurant-12,
-- ecosfera-yoga-canggu-canggu. Ожидалось 3, обновлено 3.
-- casa-blanca-bali и satoshi не тронуты — значение верное.
--
-- ecosfera-yoga-canggu-canggu и green-spot-cafe были разобраны и
-- отклонены 2026-08-04 (см. блок «ОТКЛОНЕНО» в hours-batch1-apply.sql).
-- Отклонение сработало — в применённый список они не попали. Значение
-- записал более ранний прогон, и его никто не убрал: список отклонённых
-- останавливает следующий импорт, но не убирает за предыдущим. Обе
-- страницы — при отеле ecosferahotel.com, что и объясняет 14:00 как
-- время заселения.
--
-- ── ПОЧЕМУ null, А НЕ ЗНАЧЕНИЕ ИЗ json ────────────────────────────────
--
-- У green-spot-cafe и the-nini-restaurant-12 в json стоит
-- «7.00am-11.00pm». Соблазн записать 07:00-23:00 есть, но:
--   * ни то, ни другое заведение не входило в легаси-нормализацию —
--     их json пришёл из отдельного импорта, и его источник неизвестен;
--   * проза в why_its_here повторяет ту же цифру, но она из json и
--     выведена, а не подтверждена независимо;
--   * сайты обоих недоступны из этого окружения (сетевая политика),
--     проверить неоткуда.
-- Пустое поле не отрисовывается. Это верно и чинится обычным прогоном
-- сбора по официальному источнику.

-- ── APPLY (выполнено) ─────────────────────────────────────────────────
-- ожидалось: UPDATE 3
update venues
set opening_hours = null
where slug in (
  'ecosfera-yoga-canggu-canggu',
  'green-spot-cafe',
  'the-nini-restaurant-12'
)
  and status = 'active'
  and publication_status = 'published'
  and opening_hours = 'Mo-Su 14:00-23:59';


-- ── VERIFY ────────────────────────────────────────────────────────────
-- ожидается: 3 строки с null, 2 строки с сохранённым значением.
select slug, opening_hours, opening_hours_json->'Monday' as json_monday
from venues
where slug in ('casa-blanca-bali','ecosfera-yoga-canggu-canggu','green-spot-cafe',
               'satoshi','the-nini-restaurant-12')
order by slug;
-- получено:
--   casa-blanca-bali        Mo-Su 14:00-23:59   ["2.00pm-12.00am"]
--   ecosfera-yoga-canggu    null                null
--   green-spot-cafe         null                ["7.00am-11.00pm"]
--   satoshi                 Mo-Su 14:00-23:59   ["2.00pm-12.00am"]
--   the-nini-restaurant-12  null                ["7.00am-11.00pm"]

-- Статус публикации не менялся: все пять остаются published/active.


-- ── ОСТАЁТСЯ ──────────────────────────────────────────────────────────
--
-- У green-spot-cafe и the-nini-restaurant-12 в why_its_here по-прежнему
-- написано «open daily 7:00am-11:00pm». Часы в прозе описания есть у 310
-- опубликованных заведений и проверяемы только у 38 — это отдельная
-- задача, а не следствие этой партии.
--
-- last_verified_at не трогали: дата относится к записи целиком, и другие
-- поля в тот день действительно проверялись.
--
-- ── СЛЕДУЮЩАЯ ПРОВЕРКА: сходится ли колонка со своим же источником ─────
--
-- Найдено попутно. У `nautilus` в `opening_hours` стоит
-- `Mo-Su 15:00-18:00`, а `opening_hours_json` содержит «3.00-11.00pm».
-- Текущий нормализатор из `lib/opening-hours.ts` даёт по этой строке
-- 15:00-23:00 — проверено запуском:
--
--   "3.00-11.00pm"    -> {"opens":"15:00","closes":"23:00"}
--   "2.00pm-12.00am"  -> {"opens":"14:00","closes":"23:59"}
--   "7.00am-11.00pm"  -> {"opens":"07:00","closes":"23:00"}
--
-- То есть сохранённое значение не воспроизводится из источника той же
-- строки: заведение опубликовано закрывающимся в 18:00 вместо 23:00.
--
-- Оба поля заполнены одновременно у **153 опубликованных записей**.
-- Проверка дешёвая и не требует внешнего источника: прогнать json через
-- parseRange и сравнить с колонкой. Расхождение означает, что колонка
-- написана не из этого источника — и тогда неизвестно, из какого.
-- В эту партию аудит не входил.
