# Store Assets

Committed assets:

- `app-icon-1024.png`: opaque App Store icon source.
- `rustore-icon-512.png`: opaque RuStore listing icon.

Final screenshots belong in `store-assets/screenshots/` only after the signed
native builds pass real-device QA. Required scenes and dimensions are defined
in `docs/store-submission-package.md`.
